{
  "name": "@astahmer/module-graph",
  "version": "0.14.1",
  "description": "Creates a module graph based on a given entrypoint.",
  "keywords": [],
  "license": "ISC",
  "author": "",
  "bin": "./dist/bin/index.js",
  "files": [
    "README.md",
    "dist"
  ],
  "type": "module",
  "types": "./dist/index.d.ts",
  "exports": {
    ".": {
      "types": "./dist/index.d.ts",
      "default": "./dist/index.js"
    },
    "./ModuleGraph.js": {
      "types": "./dist/ModuleGraph.d.ts",
      "default": "./dist/ModuleGraph.js"
    },
    "./utils.js": {
      "types": "./dist/utils.d.ts",
      "default": "./dist/utils.js"
    },
    "./plugins/*.js": {
      "types": "./dist/plugins/*.d.ts",
      "default": "./dist/plugins/*.js"
    }
  },
  "publishConfig": {
    "access": "public"
  },
  "scripts": {
    "clean": "node -e \"const fs=require('node:fs');const path=require('node:path');const remove=(file)=>fs.rmSync(file,{recursive:true,force:true});remove('dist');for(const file of fs.readdirSync('.')){if(/^(?:index|ModuleGraph|utils|types)(?:2)?\\.(?:js|d\\.ts|mjs|d\\.mts)$/.test(file)) remove(file);}for(const file of ['index.js','index.d.ts']) remove(path.join('bin', file));for(const file of fs.readdirSync('plugins')){if(/^(?:barrel-file|exports|imports|unused-exports)(?:2)?\\.(?:js|d\\.ts|mjs|d\\.mts)$/.test(file)) remove(path.join('plugins', file));}\"",
    "build": "pnpm clean && tsdown",
    "build:watch": "pnpm clean && tsdown --watch",
    "test": "tsx --test --experimental-test-coverage test/module-graph.test.ts",
    "test:watch": "tsx --test --watch test/module-graph.test.ts",
    "typecheck": "tsc --noEmit",
    "lint": "oxlint .",
    "lint:fix": "oxlint --fix .",
    "format": "oxfmt --write .",
    "format:check": "oxfmt --check .",
    "prepare": "lefthook install",
    "prepack": "pnpm build"
  },
  "dependencies": {
    "@rollup/pluginutils": "5.3.0",
    "@thepassle/module-utils": "0.4.0",
    "commander": "14.0.3",
    "oxc-parser": "0.125.0",
    "oxc-resolver": "11.19.1",
    "picomatch": "4.0.4"
  },
  "devDependencies": {
    "@types/node": "25.6.0",
    "@types/picomatch": "4.0.3",
    "import-meta-resolve": "4.2.0",
    "lefthook": "2.1.5",
    "oxfmt": "0.45.0",
    "oxlint": "1.60.0",
    "tsdown": "0.21.8",
    "tsx": "4.21.0",
    "typescript": "6.0.2"
  },
  "packageManager": "pnpm@10.33.0"
}
